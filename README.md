Recruitment process technical test project at Nutech.

How to run this on your local server

1. clone the repository
2. install dependencies "npm install"
3. create an .env file (example is in the repo) and setup the database link, port and jwt token there
4. run npm run dev or node src/app.jd
5. the application is running.
